apiVersion: v1
kind: Service
metadata:
  name: ai-agent-orchestrator
  labels:
    app: ai-agent-orchestrator
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8000
    protocol: TCP
    name: http
  selector:
    app: ai-agent-orchestrator

