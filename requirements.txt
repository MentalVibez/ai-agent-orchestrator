fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
boto3>=1.29.0
openai>=1.3.0
httpx>=0.25.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
slowapi>=0.1.9
python-multipart>=0.0.18
psutil>=5.9.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0

# Monitoring
prometheus-client>=0.18.0

# MCP (Model Context Protocol) — requires Python 3.10+
mcp[cli]>=1.0.0

# Optional: job queue for runs (set RUN_QUEUE_URL=redis://... to use)
arq>=0.25.0

# Optional: OpenTelemetry tracing (set OTEL_ENABLED=true to trace runs and planner steps)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
