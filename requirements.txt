fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
boto3>=1.29.0
openai>=1.3.0
httpx>=0.25.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
slowapi>=0.1.9
python-multipart>=0.0.18
psutil>=5.9.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0
psycopg2-binary>=2.9.0

# Monitoring & observability
prometheus-client>=0.18.0
python-json-logger>=2.0.7

# Production reliability
pybreaker>=1.2.0

# MCP (Model Context Protocol) — requires Python 3.10+
mcp[cli]>=1.0.0

# Optional: job queue for runs (set RUN_QUEUE_URL=redis://... to use)
arq>=0.25.0

# Optional: OpenTelemetry tracing (set OTEL_ENABLED=true to trace runs and planner steps)
opentelemetry-api>=1.20.0
opentelemetry-sdk>=1.20.0
opentelemetry-exporter-otlp-proto-http>=1.20.0

# Security: pin minimum transitive-dependency versions to satisfy CVEs.
# Keep these up-to-date when pip-audit reports new findings.
# CVE-2026-26007: cryptography < 46.0.5 — missing EC subgroup validation (ECDSA/ECDH)
cryptography>=46.0.5
# CVE-2026-21441: urllib3 < 2.6.3 — decompression bomb in redirect body drain
urllib3>=2.6.3
# CVE-2026-0994: protobuf < 6.33.5 — DoS via deeply nested Any messages in json_format
# Upper bound <7.0 required by opentelemetry-proto and googleapis-common-protos.
protobuf>=6.33.5,<7.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
pytest-mock>=3.11.1
